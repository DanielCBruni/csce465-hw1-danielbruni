---
name: safe-marker
description: describes when marker is allowed and the exact fixed command
---

# safe-marker

## Activation Rule
Use this skill ONLY when the user excplicitly runs the safe-marker tool

## Fixed Command Rule
Must only run the exact fixed command provided below:

```bash
cd ~/csce465-agentsec/hw1/bin/safe_marker.sh course-marker
```
